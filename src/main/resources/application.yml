spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/jwt
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug

jwt:
  issuer: qkrrudxkr77@gmail.com
  secret-key: jwt-test